## Packages to install on all hosts
all_hosts:
   - bridge-utils
   - ntp
   - ntpdate
   - ssh
   - sudo
   - vim

## Packages to install on deployment hosts
deployment_hosts:
   - aptitude
   - build-essential
   - git
   - python-dev
   - python-pip

## Packages to install on target hosts
target_hosts:
    - lsof
    - tcpdump
